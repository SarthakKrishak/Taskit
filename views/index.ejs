<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Taskup</title>
    <link rel="shortcut icon" href="../images/icon.png" type="image/x-icon">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 pt-10">
    <h1 class="text-white text-4xl inline-block ml-10 pt-10">Create a Task 🔽 </h1>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10">
            <form action="/create" method="post">

                <input class="block bg-zinc-800 w-1/3  px-12 text-xl py-4 mb-2 text-white rounded-md outline-none" name="title" type="text" placeholder="Title">

                <textarea class="block bg-zinc-800 w-1/3 px-12 py-4 resize-none text-xl text-white rounded-md outline-none" name="details" placeholder="Description"></textarea>

                <input class="bg-white rounded-md mt-7 text-xl px-3 py-1.5" type="submit" value="Create Task">
            </form>
        </div>

        <div class="tasks p-10 flex gap-3">
            <% if(files.length>0){ %>
                <% files.forEach(function(val){ %>
                    <div class="flex-wrap task w-72 h-screnn px-3 py-4 bg-zinc-800 rounded-md text-white tracking-tighter">
                        <h1 class="text-xl"><%= val %></h1>
                        <div class="flex justify-between mt-4">
                            <a class="text-blue-500 text-basi inline-block" href="/files/<%= val %>">Read Description</a>
                            <a class="text-blue-500 text-basic inline-block" href="/edit/<%= val %>">Edit Title</a>
                        </div>
                        
                    </div>
                <% }) %>
            <% } else { %>
                <h2 class="text-white text-xl">No Task Yet..</h2>
            <% } %>
          
        </div>


    </div>
</body>
</html>